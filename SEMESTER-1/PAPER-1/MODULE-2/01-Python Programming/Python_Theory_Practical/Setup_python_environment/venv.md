## Python Environment setup commands (Windows CMD)

* `python --version`
* `py --version`
* `mkdir my-project`	# `mkdir my-project`, `mkdir "my project"`
* `cd my-project`
* `python -m venv .venv`
* `.venv\Scripts\activate`		# should see like this -> (.venv) C:\Users\YourName\my-project>
* `python -m pip install --upgrade pip`


* `pip install requests	`
* 	or
* `pip install requests flask pandas numpy`


* `pip freeze > requirements.txt`

* `python --version`
* `pip --version`

